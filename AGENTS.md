# AGENTS.md

This repository keeps its agent instructions in `CLAUDE.md`.

Codex-specific VCS note: use normal `git` commands instead of `jj` in this
repo. Codex has spotty jj support, and git interoperates with the colocated jj
repository.

Read `CLAUDE.md` before making changes. When repository guidance changes, update
`CLAUDE.md` as the canonical source instead of duplicating instructions here.

# Add-zero lemma

Replace `Proof.lean` with a complete Lean proof of the hidden theorem. The theorem name
and type are checked by a controller-owned hidden file, not by this brief.

Constraints:
- Keep the exported theorem name `add_zero_candidate`.
- Do not use `sorry`, `axiom`, `unsafe`, or external imports.
- The proof checker is authoritative; model self-judgment does not count.
